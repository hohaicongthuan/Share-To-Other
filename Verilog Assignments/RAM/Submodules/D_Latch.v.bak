module DLatch(D, E, Q, Q_N);
	input D, E;
	output Q, Q_N;
	
	// Internal wires
	wire w1, w2, w3, w4, D_N;
	
	not(D_N, D);
	and(w1, D_N, E);
	and(w2, D, E);
	xnor(Q, w1, Q_N);
	xnor(Q_N, w2, Q);
endmodule